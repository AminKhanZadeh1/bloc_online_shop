import 'package:bloc_online_shop/Config/Theme/Colors/g_color.dart';
import 'package:flutter_spinkit/flutter_spinkit.dart';

const whiteSpinkit = SpinKitCircle(
  color: GColor.white,
  size: 50,
);

const whiteLoadingSpinkit = SpinKitCircle(
  color: GColor.white,
  size: 25,
);

const blackSpinkit = SpinKitCircle(
  color: GColor.white,
  size: 50,
);

const blackLoadingSpinkit = SpinKitCircle(
  color: GColor.white,
  size: 25,
);
